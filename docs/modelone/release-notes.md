# modelOne 0.1.0-dev 版本说明

日期：2026-09-21。发布标签建议 `modelone-v<主版本>.<次版本>.<修订号>`；不兼容接口或数据变化升级主版本，功能迭代升级次版本，修复升级修订号。正式标签仅在验收通过后创建。

本次改动：统一品牌配置及运行时公开配置；三个前端品牌入口；服务端导航、错误页和展示文案；初始化及示例数据；资源清单和企业仓库解析；保留兼容标识的数据迁移；部署渲染和发布扫描；交付文档与许可证收集工具。

修复此前批量替换引入的问题：文生图 `$text` 和多轮对话占位符恢复，知识库文件路径对齐，容器挂载目录恢复兼容路径，迁移不再重命名作业或吞掉 SQL 错误，Kubernetes 品牌标签不再修改不可变选择器。

本地验证：三个生产前端构建成功；五项迁移/配置/扫描回归检查通过；产品源码及编译包品牌扫描通过；生产构建无 source map；Compose 配置和 Kustomize 清单解析通过。现有构建仍有 lint/依赖与包体积警告。

待验收：自有 Git 仓库切换、正式品牌资产、938 个原资源引用和 144 个原镜像引用的筛选与迁移、最终容器 SBOM、生产凭据和域名/TLS、认证、真实全新安装和升级、业务功能回归、离线与备份恢复验证。清单数量含历史示例，不等于最终运行依赖数量。

禁止将此开发基线描述为“全部验收通过”。

代码管理：开发分支为 `codex/modelone-brand-baseline`，原仓库已改为只读 `upstream`（禁用 push）。自有仓库地址未提供，尚未设置新的 `origin` 或创建正式发布标签。
