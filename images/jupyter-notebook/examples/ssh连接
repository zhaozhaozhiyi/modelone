# 将此文件内容追加到 ~/.ssh/config；密码由 NOTEBOOK_ROOT_PASSWORD 注入
# ssh-copy-id -p PORT root@HOST  本地设置免密登录

Host modelone
  HostName localhost
  Port localport
  User root
  ServerAliveInterval 10
  ControlMaster auto
  ForwardX11 yes
  ForwardX11Trusted yes
  ForwardAgent yes
